package com.gayanukaa.contentcalendar.model;

//enum should be defined in their own file
//enum can be defined inside a class
//enum can be used as a standalone class
public enum Type {
    ARTICLE,
    VIDEO,
    COURSE,
    CONFERENCE_TALK
}
